--- tests/e2e/scopeweave.spec.js
+++ tests/e2e/scopeweave.spec.js
@@ -1118,6 +1118,15 @@
 });

 test.describe('ScopeWeave Planner - Palette UX Enhancements', () => {
+  test('adds helpful tooltips to footer action buttons', async ({ page }) => {
+    await page.goto('./');
+
+    const addRootBtn = page.locator('#add-root-task');
+    await expect(addRootBtn).toHaveAttribute('title', '새로운 최상위(1단계) 작업을 추가합니다.');
+
+    const importCsvBtn = page.locator('#import-csv');
+    await expect(importCsvBtn).toHaveAttribute('title', 'CSV 파일에서 작업을 불러와 기존 데이터를 덮어씁니다.');
+  });
+
   test('adds helpful placeholder to project name input', async ({ page }) => {
     await page.goto('./');
