apiVersion: v1
kind: Service
metadata:
  name: scopeweave-service
  namespace: scopeweave
spec:
  selector:
    app: scopeweave
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
