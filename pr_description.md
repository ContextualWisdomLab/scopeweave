## 🚨 심각도
HIGH

## 💡 취약점
웹훅 생성 시 URL 검증이 단순히 HTTP/HTTPS 스킴(scheme)만 확인하고 있어, 공격자가 루프백 주소(`127.0.0.1`, `localhost`)나 내부 네트워크 대역의 호스트를 입력해 서버 측 요청 위조(SSRF) 공격을 수행할 수 있었습니다.

## 🎯 영향
서버(ScopeWeave 백엔드)를 프록시로 악용하여 외부에 노출되지 않은 내부 인프라망 포트나 내부 API에 접근하여 정보 유출 및 내부망 스캔, RCE 등을 트리거할 수 있는 잠재적 위험이 존재했습니다.

## 🔧 수정
웹훅 URL 검증(`server/app.mjs`)에 다음을 추가했습니다.
- `new URL(url)`로 URL을 파싱하여 올바른 형태인지 확인합니다.
- 프로토콜이 정확히 `http:` 또는 `https:`인지 검사합니다.
- 해석된 `hostname`이 루프백(`localhost`, `127.0.0.1`, `::1`, `0.0.0.0`)이거나 내부망을 의미하는 `.local`로 끝나는 경우 생성을 차단합니다.

## ✅ 검증
- URL 파싱 오류 시 400 에러를 반환합니다.
- `http://127.0.0.1:9/hook`과 같은 로컬 주소 요청 시 SSRF 방어 로직에 의해 차단되도록 API Smoke 테스트(`tests/api/smoke.mjs`) 코드를 외부 URL(`example.com`)로 업데이트하여 테스트를 통과(Pass)시켰습니다.
